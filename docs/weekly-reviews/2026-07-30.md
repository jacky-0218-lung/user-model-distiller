# 每週診斷報告 — 2026-07-30 (UTC)

本檔為 `user-model-distiller` 每週改進的連續性紀錄。每次於全新雲端環境自動執行，無先前對話記憶，故此檔同時作為「上週狀態 → 本週改動 → 下週待辦」的接力棒。

## 1. 上次狀態摘要

- 上一份報告：`docs/weekly-reviews/2026-07-26.md`。該週的「隱形字元 context poisoning 三層防禦 + Skill 目錄排除說明」已合併為 `0b8860f`（#13）。
- 開工檢查結果：
  - 遠端除 `main` 外只有三個 dependabot 分支（actions/checkout、codeql-action init/analyze），**無**進行中的 `weekly-improve-*` 分支，無撞車風險。dependabot 分支屬 GitHub Actions 版本升級，非本任務範圍，留待維運者處理。
  - 現有 tag：`v0.2.0` → `v0.2.3`。上週建議的 `v0.3.0` 尚未發版（發版需維運者在本機執行 build/verify 與簽署 tag，非本任務可代行）。
  - 基準線（改動前，本環境）：`pytest` **110 passed, 18 subtests**、`unittest discover` **110 OK**、`tools/check_repository.py` = **ok**。與上週合併後狀態一致，無回歸。
- 上週待辦點名佇列第 3 項（疑難排解段落）或第 5 項（規則挑選指引）。兩項皆為低風險文件改動，本週**兩項都做**。

## 2. 本週研究發現與來源

### 方向 A — Skill 撰寫技巧與平台變化

- Anthropic《Skill authoring best practices》本週複查無實質變更；本 repo 的 SKILL.md（200 行，< 500 建議上限）、description（第三人稱、做什麼＋何時用）仍符合現行建議，無需改動。
  - 來源：https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
- Codex changelog 複查：6 月 4 日（0.137.0）／6 月 8 日（0.138.0）／6 月 9 日（0.139.0）之後，頁面上未見與 skill／memory／AGENTS.md 相關的新破壞性變更。0.138.0 的「skills plumbing 移入獨立 crates」與 0.139.0 的「AGENTS.md 經環境檔案系統載入路徑修正」皆為內部重構，不影響本 repo 的安裝與觸發方式。
  - 來源：https://developers.openai.com/codex/changelog
- `openai/skills#420`（skill 安裝目錄 `.codex/skills` vs `.agents/skills` 不一致）**仍 Open**，自 2026-05-22 開立以來無 maintainer 回覆、無 label、無關聯 PR。README 現有的排除說明維持有效，佇列第 7 項續留追蹤。
  - 來源：https://github.com/openai/skills/issues/420

### 方向 B — 競品與類似工具（memory / personalization）

- Mem0《State of AI Agent Memory 2026》：業界評測已收斂到 LoCoMo／LongMemEval／BEAM 三套基準；領先系統的最大增益來自**時間推理**（+29.6）與多跳查詢（+23.1）。與本 repo 直接相關的三點：
  1. **多訊號檢索**（語意 + BM25 + 實體）取代單一語意相似度已成共識——「純語意檢索會漏掉措辭不同但適用的規則」正是本週改動 2 在 SKILL.md 補上確定性挑選指引的依據。
  2. **Actor-aware storage**（使用者的話與 agent 推論分開歸屬）被列為關鍵能力——本 repo「僅使用者本人話語可成為證據」的設計早已內建更嚴格的版本。
  3. **Memory staleness**（高頻取用的記憶在使用者情況改變後「自信地錯」）被明確承認為業界未解問題——本 repo 的 supersede + 到期時間 + digest-bound 重審機制即是治理面解法，佇列第 4 項（時間有效性語意）價值獲得再次佐證。
  - 來源：https://mem0.ai/blog/state-of-ai-agent-memory-2026
- arXiv《Toward User-Conditioned Evaluation of Personal LLM Agents under Temporal Interventions》（2607.21635）：學界開始把「偏好隨時間變化」納入 personal agent 的評測條件，方向與佇列第 4 項一致，列為後續設計參考。
  - 來源：https://arxiv.org/html/2607.21635v1

### 方向 C — 安全與隱私強化

- OWASP《Top 10 for Agentic Applications》相關報告：prompt injection 對映到十類中的六類，仍是 agentic 失敗的最大宗；報告引述兩個設計啟發式——「Lethal Trifecta」（私有資料 + 不受信任內容 + 對外通訊三者兼具即成外洩工具）與 Meta「Agents Rule of Two」（無人工核准時三者至多取二）。對照本 repo：正常流程只具備「私有資料 + 不受信任內容」兩項,對外通訊被 external-review 閘門與獨立揭露核准擋住,符合 Rule of Two;此對照已足夠,無需新改動。2026-03 的 LiteLLM 供應鏈事件(3 小時內近 4.7 萬次下載惡意版本)再次佐證上週 CI 守門的價值。
  - 來源：https://www.helpnetsecurity.com/2026/06/11/owasp-prompt-injection-ai-security-failures/
- Snyk《ToxicSkills》與 Unit 42《Trust No Skill》為上週已引用來源，本週複查無新版本。
  - 來源：https://snyk.io/blog/toxicskills-malicious-ai-agent-skills-clawhub/ 、https://unit42.paloaltonetworks.com/ai-agent-supply-chain-risks/

### 方向 D — 文件與易用性（**本週改動來源**）

- 上週端對端實測與程式碼盤點確認：工作流程有三種使用者會實際遇到的「停下來」狀態——`privacy_blocked`（exit 3，依設計阻擋）、路徑安全拒絕（exit 2，`error: ...`）、`verify` 失敗（篡改／不完整）——但 README 沒有任何一處告訴使用者「下一步該做什麼」。對一個把「預設拒絕」當賣點的工具，缺少「被拒絕之後怎麼辦」的說明是最大的易用性缺口，也最容易誘使使用者（或 agent）繞過安全機制自行修補。此即本週改動 1。
- 依程式碼確認的事實：`distill_workflow.py` preview 於 `privacy_blocked` 回 exit 3、路徑／格式錯誤回 exit 2；`verify` 失敗訊息如 `Artifact hash mismatch`；`prepare_review_pack.py` 於 `blocked` 時不產生任何 pack/mapping 檔。疑難排解內容全部以這些實際行為為準，未杜撰任何訊息文字。

## 3. 更新後的改進佇列（依價值排序）

1. **[本週已做]** README 新增中英雙語「疑難排解」段落。（價值：中高；工作量：低）
2. **[本週已做]** SKILL.md 第 8 節補確定性規則挑選指引。（價值：中；工作量：低）
3. 借鏡 Zep／Mem0 的時間有效性發現，評估在 `profile-schema.md` 更明確化偏好的起訖與變更歷程檢索語意。（價值：中；工作量：中；**必須單獨一個 PR**，牽動 schema 版本；arXiv 2607.21635 可作評測設計參考）
4. 在 `tools/skill_bundle.py` 的 receipt 輸出中一併回報「已掃描且未發現不可見字元」，讓高保證安裝的 receipt 自帶這項聲明。（價值：中；工作量：低〜中，涉及 receipt 欄位與測試）
5. 追蹤 `openai/skills#420`；若上游確定 `.agents/skills` 為唯一標準，README 與 install.md 需同步改寫。（本週已複查：仍 Open 無回覆）
6. 發版 `v0.3.0`：#13 已合併,包含正規化器行為變更(`normalize_sessions` 0.4.0),同一輸入的 `normalized.jsonl` 與 `redaction_count` 會與舊版不同,建議 release notes 明示。（需維運者本機執行,無法由本任務代行）
7. 為長參考檔加目錄——references 皆 < 100 行，續留觀察。（價值：低）

## 4. 本週實際改動與驗收

### 改動 1：README 中英雙語「疑難排解」段落

於「私有工作流程／Private workflow」與「安全模型／Security model」之間各插入一節，涵蓋六種實際情境，內容全部對照程式碼行為撰寫：

1. 找不到 Python 或版本太舊（Windows `py -3`、Codex 內建 workspace Python；明確警告不要讓 agent 以重新實作腳本的方式繞過安全檢查——此警告與 SKILL.md 第 3 節既有語句一致）。
2. Skill 安裝後未觸發（開新 session、依「手動安裝」查另一個目錄、確認未多包一層目錄）。
3. `privacy_blocked`（exit 3）：說明這是依設計阻擋；只看聚合 `privacy-report.json`、不開被擋內容；處置方向與「必須重跑到新輸出目錄」。
4. `error: ...`（exit 2）：列出會被拒絕的路徑型態（已存在、位於 repo 樹、重疊、symlink、雲端同步），說明拒絕是刻意的。
5. `verify` 失敗（如 `Artifact hash mismatch`）：視為不可信、不要手動修補、重跑而非復原。
6. 外部審查資料包 `blocked`：說明此時不會產生任何 pack/mapping 檔，改用本機模型或人工審查，不要手工組揭露副本。

每一條的立場一致：**解釋「為什麼被擋是對的」再給出安全的下一步**，而不是教使用者繞過。未改動任何既有段落（`git diff` 確認純插入,28 行）。

### 改動 2：SKILL.md 第 8 節規則挑選指引

原文只說「use a compact core plus at most three to five task-relevant rules」，未定義「task-relevant」如何挑選，實務上 agent 會退回語意相似度。補上兩句：先取 scope 與當前 project／task／temporary 情境相符者，再取 `category` 與工作類型相符的全域規則（範例用 schema 實際允許的 `response_style`、`coding`，經 `ALLOWED_CATEGORIES` 核對）；並明確警告不要只依語意相似度——措辭與任務不同的規則仍可能是適用的那條。依據為方向 B 中 Mem0／Hindsight 的多訊號檢索共識。SKILL.md 維持 200 行。

**未改動**：核心原則、schema、腳本行為、測試、CI/release workflow 一律未動。本週為純文件改動。

### 驗收結果

- `pytest -q` → **110 passed, 18 subtests passed**（與基準線一致，無回歸）。
- `python3 -m unittest discover -s tests` → **Ran 110 tests, OK**。
- `python3 tools/check_repository.py` → **repository guard: ok**（含上週新增的隱形字元守門，證明本週新增文字乾淨）。
- `python3 -m compileall -q skills tests tools` → **ok**。
- 疑難排解中引用的 exit code 與訊息字串（3／2／`Artifact hash mismatch`／`blocked` 不產檔）逐一對照 `distill_workflow.py`、`privacy_guard.py`、`prepare_review_pack.py` 原始碼確認。

## 5. 下週待辦

- 佇列第 3 項（時間有效性語意）為目前最有價值的實質功能項，但牽動 schema 版本，動工前先設計、且**單獨一個 PR**；或改做第 4 項（bundle receipt 掃描聲明）作為較小的紮實改動。
- 開工時先讀本檔，並以 `git ls-remote --heads` 檢查是否已有 `weekly-improve-*` 分支。
- 續追 `openai/skills#420`。
- 提醒維運者：`v0.3.0` 發版與三個 dependabot 分支（checkout 7.0.1、codeql-action 4.37.3 ×2）仍待處理。

## 6. 本週環境限制紀錄

- 與前兩週相同：此環境的 GitHub REST 與 GraphQL API 對本 repo 皆未開通（GraphQL 回「only the pinned set of PR-review operations is served」、REST 回「GitHub access to this repository is not enabled for this session」）。`gh` 2.45.0 已安裝但無法自動開 PR；`git push`（git 協定 + token URL）可用，本週分支已推送。
- PR 採後備路徑：

  **一鍵開 PR：** https://github.com/jacky-0218-lung/user-model-distiller/compare/main...weekly-improve-20260730?expand=1

  **建議 PR 標題：** 每週改進 2026-07-30：README 疑難排解段落 + SKILL.md 規則挑選指引

  **建議 PR 內文重點：** (1) README 新增中英雙語「疑難排解」段落，涵蓋六種實際情境（Python runtime、Skill 未觸發、`privacy_blocked` exit 3、路徑拒絕 exit 2、`verify` 失敗、外部審查包 `blocked`），每條先解釋預設拒絕的理由再給安全下一步，全部對照原始碼行為撰寫。(2) SKILL.md 第 8 節補確定性規則挑選指引：先 scope 後 category，不只依語意相似度，依據 Mem0 2026 多訊號檢索共識。純文件改動；驗收 pytest 110 passed、unittest 110 OK、repository guard ok、compileall ok。
- 給維運者的設定建議（連續第三週）：若要讓此每週任務能自動開 PR，需為此執行環境開通 GitHub API 存取。
