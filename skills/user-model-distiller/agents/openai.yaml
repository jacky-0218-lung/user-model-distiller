interface:
  display_name: "User Model Distiller"
  short_description: "Build and continuously use reviewable preferences"
  default_prompt: "Use $user-model-distiller to build an approved profile and set up safe cross-session use."
